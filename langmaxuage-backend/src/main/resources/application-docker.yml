spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/toeic_db
    username: postgres
    password: postgres

  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    root: INFO